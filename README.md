Bus Ticket Management System simulates a virtual passenger queue at a bus ticket booth. It handles passengers one by one in a queue that is simulated by using a linked list data structure.

STEPS TO COMPILE:
1. g++ *.cpp -o bus_system
2. .\bus_system.exe (Windows) OR ./bus_system (LINUX)
